random_test.py : random classの使い方に関するsampleコード.

[実行]
	python random_test.py

[メモ]
●random classを使って、擬似乱数生成を行う.
○floatの擬似乱数値
	import random

	random.random() 	... 0.0～1.0までのfloat値を取得します。
	random.uniform(0,100)	... 0～100までのfloat値を取得します。
	random.randint(0,100)	... 0～100までのint値を取得します。
	random.choice('1234567890abcdefghij')	... 1234567890abcdefghij内から一つの要素を取得します。

	sample_list = ['python', 'izm', 'com', 'random', 'sample']
	random.shuffle(sample_list) ... array内の要素をシャッフルします

